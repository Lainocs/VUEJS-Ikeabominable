import { createStore } from 'vuex';

import { user } from './modules/user.module';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user
  },
});
